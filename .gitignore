# tool-local state, not project content
.memsearch/

# node
node_modules/
dist/

# Nx local cache (machine-local, regenerable)
.nx/

# runtime data (clipsync-server writes SQLite + blobs here)
data/

# local HTTPS cert/key generated by mkcert (machine- and IP-specific, never
# shared — see apps/clipsync-server/docs/LOCAL_HTTPS.md and scripts/dev-lan.mjs)
*.pem

# misc
*.log
.DS_Store
.env
.env.local
.worktrees/
.claude/
docs/
